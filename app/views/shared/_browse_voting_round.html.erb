<%= link_to "Voting for " + @previous_voting_round.label, voting_round_path(@previous_voting_round) unless @previous_voting_round.nil? %>
<%= link_to "Voting for " + @next_voting_round.label, voting_round_path(@next_voting_round) unless @next_voting_round.nil? %>
