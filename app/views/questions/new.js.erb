$('document').ready(function() {
  $modal = $('#myModal'),
  $('.modal-header').html("Submit your question to Curious City");
  $('.modal-body').html("<%= escape_javascript(render "new") %>");
  $modal.modal('show');
});
