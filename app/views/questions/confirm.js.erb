$('document').ready(function() {
  $('.modal-backdrop').remove();
  $('#ask-question-modal').html("<%= escape_javascript(render "confirm") %>");
  $('#modal-form-submit').on('click', function(e){
    e.preventDefault();
    $('#new_question').submit();
  });
//$('#modal-form-back').on('click', function(e){
//  e.preventDefault();
//  $('#new_question').attr('url', '<%= { action: picture_question_path } %>');
//  $('#new_question').submit();
//});
  $('#confirm-question-modal').modal('show');
});
